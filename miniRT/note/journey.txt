tuples operations
	equal 			//compare two floating point numbers
	add_tuples
	subtract_tuples
	negate_tuple
	multiply_tuple
	divide_tuple
	magnitude
	normalize
	dot_product
	cross_product

matrix operations


ray-sphere intersections
	create_ray	// (origin, direction)
	position	// ray.origin + ray.direction * t

	intersection	// i(t, obj.id)
	intersections	// a list of i

	transform	// world space -> object space