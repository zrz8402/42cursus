Deadline:
3.2 Friday - finish built-in commands (basic)


built_in commands
- echo ~
- cd
- pwd ~
- export
- unset ~
- env ~
- exit



change lex_list to
  redir array
  cmd array

pipes
  pipe()
  need to close it
pids
  fork()
  waitpid()
cleanup


ONLY change pwd and oldpwd in both envp and envlst
->cd

export -> change envlst